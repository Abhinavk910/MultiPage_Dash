module.exports = {
    darkMode: 'class',
    content: [
      "./**/*.py",  // Include all Python files
      "./assets/**/*.{html,js}"  // Include files in assets folder if needed
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }